# Development defaults for Vite.
# Real `/api` remains primary; on failure (no backend/DB), serve frontend mock data.
# Set to "force" to skip the network entirely, or "false" to disable mocks.
VITE_USE_MOCK_DATA=true
